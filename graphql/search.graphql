query Search($query: String!) {
  users: search(query: $query, first: 10, type: USER) {
    nodes {
      ... on User {
        name
        avatarUrl
        bio
        id
        location
        url
        login
      }
    }
    pageInfo {
      endCursor
      hasNextPage
      hasPreviousPage
    }
  }
  repositories: search(query: $query, first: 10, type: REPOSITORY) {
    nodes {
      ... on Repository {
        id
        nameWithOwner
        createdAt
        description
        stargazerCount
        url
        updatedAt
        licenseInfo {
          name
        }
        languages(first: 1) {
          nodes {
            name
            color
          }
        }
      }
    }
    pageInfo {
      endCursor
      hasNextPage
      hasPreviousPage
    }
  }
}
